block('tabs')(
    tag()('ul'),
    js()(true),
    content()(function() {
        var tabs = [],
            content = [];

        tabs.push(this.ctx.content.map(function(elem) {
            content.push({
                block : 'content',
                mix : [{ mods : { 'box' : elem.title }}],
                attrs : { id : elem.title }
            });

            return {
                elem : 'tab',
                content : elem.title
            }
        }));

        tabs.push({
            block : 'tabContent',
            content : content
        });

        return tabs;
    })
);

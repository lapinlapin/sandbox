block('tabs')(
    tag()('ul'),
    js()(true),
    content()(function() {
        var tabs = [],
            content = [];

        tabs.push(this.ctx.content.map(function(elem, i) {
            content.push({
                block : 'content',
                mix : [{ mods : { 'box' : elem.title }}],
                attrs : { id : elem.title }
            });

            return {
                elem : 'tab',
                elemMods : i === 0 ? { selected : true } : { selected : false },
                content : elem.title
            }
        }));

        tabs.push({
            block : 'tabContent',
            content : content
        });

        return tabs;
    })
);
